# Python dependencies for the project (s1–s4 Python components)
# Install with:  pip install -r requirements.txt
# Freeze exact versions after installation:  pip freeze > requirements.lock.txt

GEOparse>=2.0.3      # GEO SOFT download and parsing
pandas>=2.0          # table and merge support
mygene>=3.2.2        # Ensembl -> gene symbol (s4 merge)
numpy>=1.24
scikit-learn>=1.9.0
matplotlib>=3.11.1
scipy>=1.10           # hierarchical clustering and distance computations
shap>=0.42.0          # SHAP explanations for model interpretation
xgboost>=1.8.0        # optional: required for scripts/s5_branch2_ml/05_trees_shap.py
